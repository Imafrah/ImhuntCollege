services:
  - type: web
    name: collegehunt-api
    runtime: docker
    healthCheckPath: /api/health
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: PORT
        value: 3000
      - key: ADMIN_API_KEY
        sync: false
